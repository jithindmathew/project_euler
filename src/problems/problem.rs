#[allow(unused_imports)]
use crate::{maths, time_solutions};

pub fn solve() {
    println!("PROBLEM NOT SOLVED");

    // println!("{:?}", maths::get_power_of_a_number(0, 1000));
    // println!("{:?}", maths::get_power_of_a_number(1, 1));
    // println!("{:?}", maths::get_power_of_a_number(1, 0));
    // println!("{:?}", maths::get_power_of_a_number(1, 91));
    println!(
        "{:?}",
        maths::multiply_two_numbers_as_vec(vec![9, 9, 9], vec![9, 9],)
    );
}
